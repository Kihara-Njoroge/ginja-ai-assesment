# Application
APP_NAME=Ginja AI
APP_VERSION=0.1.0
DEBUG=false

# Server
HOST=0.0.0.0
PORT=8000

# CORS — comma-separated origins (use * for development)
ALLOWED_ORIGINS=["*"]

# Logging — DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
